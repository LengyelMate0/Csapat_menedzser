<Window x:Class="Focicsapat_menedzser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Focicsapat Menedzser - Magyar Válogatott" Height="650" Width="900"
        Background="#C0392B">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEEEEE"/>
            <Setter Property="RowHeight" Value="38"/>
            <Setter Property="AlternatingRowBackground" Value="#FBFBFB"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>

        <Style x:Key="WhiteGreenButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#27AE60"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#27AE60"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LightRedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="logo.png" Height="60" Margin="0,0,20,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="Magyar Válogatott - Keretkezelő" FontSize="28" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="8">
            <DataGrid x:Name="dgJatekosok" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Név" Binding="{Binding Nev}" Width="*"/>
                    <DataGridTextColumn Header="Poszt" Binding="{Binding Poszt}" Width="150"/>
                    <DataGridTextColumn Header="Gólok" Binding="{Binding Golok}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#E74C3C"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Mérkőzések" Binding="{Binding Meccsek}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#27AE60"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" Background="#22FFFFFF" CornerRadius="12" Padding="20" Margin="0,25,0,15">
            <StackPanel>
                <TextBlock Text="Új játékos felvétele (Limit: 26 fő)" FontWeight="Bold" FontSize="18" Margin="5,0,0,20" Foreground="White"/>
                <UniformGrid Columns="5">
                    <StackPanel Margin="5">
                        <TextBlock Text="Név:" FontSize="12" Margin="5,0,0,3" Foreground="#DDDDDD"/>
                        <TextBox x:Name="txtNev" Padding="8" BorderBrush="White" BorderThickness="1" Background="#11FFFFFF" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Poszt:" FontSize="12" Margin="5,0,0,3" Foreground="#DDDDDD"/>
                        <TextBox x:Name="txtPoszt" Padding="8" BorderBrush="White" BorderThickness="1" Background="#11FFFFFF" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Gólok:" FontSize="12" Margin="5,0,0,3" Foreground="#DDDDDD"/>
                        <TextBox x:Name="txtGolok" Text="0" Padding="8" BorderBrush="White" BorderThickness="1" Background="#11FFFFFF" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Meccsek:" FontSize="12" Margin="5,0,0,3" Foreground="#DDDDDD"/>
                        <TextBox x:Name="txtMeccsek" Text="0" Padding="8" BorderBrush="White" BorderThickness="1" Background="#11FFFFFF" Foreground="White"/>
                    </StackPanel>
                    <Button Content="Hozzáadás" Click="BtnHozzaad_Click" Style="{StaticResource WhiteGreenButtonStyle}" Height="45" Margin="10,22,5,0" VerticalAlignment="Bottom"/>
                </UniformGrid>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Kijelölt játékos törlése" Click="BtnTorles_Click" Style="{StaticResource LightRedButtonStyle}" Width="220" Height="45"/>
        </StackPanel>
    </Grid>
</Window>